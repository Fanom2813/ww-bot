# ww-bot

## Frontend tooling
- Use **bun** for all frontend package/script commands (never npm/npx/yarn/pnpm).
  - Install: `bun install`
  - Build: `bun run build`
  - Typecheck: `bun tsc` (run from `frontend/`)
